#include <iostream>
#include <vector>
using namespace std;
using std::vector;

int main() {
	vector<int> v = { 1,2,3,4 };
	int n;
	cin >> n;
	for (auto it = v.begin(); it != v.end(); ++it) {
		*it = *it + n;
	}
	for (auto it = v.begin(); it != v.end(); ++it) {
		cout << *it << " ";
	}
}
